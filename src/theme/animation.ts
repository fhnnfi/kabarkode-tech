/** Token animasi (§73): durasi pendek, ease-out, tanpa bounce. */
export const animation = {
  duration: {
    fast: 150,
    normal: 250,
    slow: 400,
  },
  easing: 'cubic-bezier(0.16, 1, 0.3, 1)',
} as const;
